<Project>
  <PropertyGroup>
    <!-- installer.ps1 rejects a payload containing loose .pdb files: a Setup.exe
         should not ship debug symbols, and the single-file publish would carry
         them alongside the executable. Embedded symbols keep stack traces. -->
    <DebugType>embedded</DebugType>
  </PropertyGroup>
</Project>
